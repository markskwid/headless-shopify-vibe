# Shopify: use your permanent myshopify.com domain.
SHOPIFY_STORE_DOMAIN=your-store.myshopify.com

# Public canonical URL for metadata, structured data, robots.txt, and sitemap.xml.
# Use the deployed headless storefront domain, not the myshopify.com domain.
STOREFRONT_BASE_URL=https://www.example.com

# Recommended for server-rendered storefronts. Keep this value secret.
# Legacy account pages also require customer read/write Storefront permissions.
SHOPIFY_STOREFRONT_PRIVATE_ACCESS_TOKEN=

# Optional alternative for public Storefront API access.
SHOPIFY_STOREFRONT_PUBLIC_ACCESS_TOKEN=

# Optional. Defaults to the stable version used by this starter.
SHOPIFY_STOREFRONT_API_VERSION=2026-07

# Shopify app client secret used only to verify HTTPS webhook HMAC signatures.
# This is not a Storefront API token and must never use a NEXT_PUBLIC_ prefix.
SHOPIFY_WEBHOOK_SECRET=

# Sanity project identifiers are public and safe to expose to the browser.
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production

# Only needed for private datasets or draft content. Never prefix this with NEXT_PUBLIC_.
SANITY_API_READ_TOKEN=
SANITY_API_VERSION=2026-08-27

# Shared secret used to sign Sanity GROQ-powered webhook deliveries.
# Generate at least 32 random characters and configure the same value in Sanity.
SANITY_REVALIDATE_SECRET=

# Klaviyo newsletter integration. Keep the private API key server-only.
# The private key needs lists:write, profiles:write, and subscriptions:write scopes.
KLAVIYO_LIST_ID=
KLAVIYO_API_KEY=

# Security and abuse protection. All values are server-only.
# Configure Upstash in production so limits are shared by every app instance.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
# Generate at least 32 random characters; changing it resets rate-limit buckets.
RATE_LIMIT_KEY_SALT=

# This header must be overwritten by your trusted reverse proxy/CDN.
# Vercel: x-vercel-forwarded-for; Cloudflare: cf-connecting-ip.
TRUSTED_PROXY_IP_HEADER=x-forwarded-for

# Usually blank. Add comma-separated hostnames only when a trusted reverse
# proxy intentionally changes the Server Action origin/host relationship.
SERVER_ACTION_ALLOWED_ORIGINS=

# Playwright E2E tests. Put real development/staging values in the ignored
# .env.e2e.local file. Never point these tests at production services.
E2E_BASE_URL=http://127.0.0.1:3100
E2E_PRODUCTION_HOSTNAME=production-storefront.example.com
E2E_PRODUCT_HANDLE=available-test-product
E2E_UNAVAILABLE_PRODUCT_HANDLE=sold-out-test-product
E2E_COLLECTION_HANDLE=test-collection
E2E_SEARCH_TERM=test product
E2E_EDITORIAL_SLUG=test-page
E2E_EDITORIAL_HEADING=Test page
E2E_CHECKOUT_HOSTNAME=test-store.myshopify.com

# Explicit approvals must exactly match the corresponding development/staging
# app values before Playwright will start.
E2E_APPROVED_SHOPIFY_STORE_DOMAIN=test-store.myshopify.com
E2E_APPROVED_SANITY_PROJECT_ID=test-project-id
E2E_APPROVED_SANITY_DATASET=test-dataset

# Optional development-store customer. Set both or neither. Authentication E2E
# tests remain deferred until a dedicated test customer is approved.
E2E_CUSTOMER_EMAIL=
E2E_CUSTOMER_PASSWORD=
